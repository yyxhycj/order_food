# 迁移记录

日期：2026-05-14

## 已完成

1. 新增 `constants/`，统一集合名、角色、菜谱状态和请求状态。
2. 新增 `services/`，页面不再直接调用 Express/MySQL 接口。
3. 新增 `cloudfunctions/`，覆盖登录、菜谱写操作、想吃请求和收藏。
4. 新增可复用组件：
   - `recipe-card`
   - `recipe-form`
   - `request-card`
   - `empty-state`
   - `loading-state`
   - `user-avatar`
5. 新增第一版主线页面：
   - `pages/home/home`
   - `pages/recipe/detail/detail`
   - `pages/recipe/edit/edit`
   - `pages/requests/list/list`
   - `pages/requests/manage/manage`
   - `pages/user/profile/profile`
   - `pages/admin/admin/admin`
   - `pages/admin/categories/categories`
   - `pages/admin/recipes/recipes`
6. `app.json` 已收敛为三个 tab：菜谱、想吃、我的。
7. `app.js` 已移除购物车、订单、localhost API 等旧点单系统全局逻辑。

## 已删除的旧主线

旧点单系统、旧 Express/MySQL 后端、盲盒、提醒、游戏化等不属于第一版核心闭环的内容不再保留。

## 后续验收

1. 在微信开发者工具中部署云函数。
2. 开通云数据库集合并配置权限。
3. 使用真机调试完成登录、发布、详情、收藏、想吃请求、请求处理闭环。
