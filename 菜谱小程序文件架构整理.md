# 菜谱小程序文件架构整理

日期：2026-05-14

## 1. 整理目标

本文件用于把当前“点单系统遗留结构”整理为“朋友菜谱小程序结构”。

整理目标有三个：

1. 让目录结构直接表达新业务：菜谱、想吃请求、收藏、用户、管理。
2. 让旧业务逐步退出主线：商品、购物车、订单、价格、库存、店铺。
3. 让后续 CloudBase 迁移有明确落点，不在旧 Express/MySQL 结构里继续加复杂度。

本次整理采用“先定目标结构，再分阶段迁移”的方式。当前工作区已有未提交改动，因此不立即大规模移动或删除页面文件，避免把已有改动打散。后续实现时按本文档逐步迁移。

## 2. 当前目录问题

当前目录里同时存在三套业务语义：

```text
点单系统：
menu / product / cart / order / orders / admin/orders

菜谱系统：
pages/recipes/*
server/models/Recipe*

扩展玩法：
blind-box / reminders / game
```

主要问题：

1. 页面命名和新产品定位不一致：`cart`、`order`、`product` 仍然是点单系统语义。
2. 前后端路径不一致：页面中同时存在 `/products`、`/orders`、`/dishes`、`/requests`。
3. 数据模型不一致：`dish_name`、`product_name`、`name` 混用。
4. 后端还保留 MySQL 和 Express，但第一版目标已经转向 CloudBase。
5. 扩展功能过早进入主目录：盲盒、提醒、游戏化会干扰第一版主链路。

## 3. 目标根目录结构

最终建议整理为：

```text
little/
├── app.js
├── app.json
├── app.wxss
├── project.config.json
├── sitemap.json
├── docs/
│   ├── 菜谱小程序整体方案.md
│   ├── 菜谱小程序文件架构整理.md
│   └── 迁移记录.md
├── cloudfunctions/
│   ├── login/
│   ├── createRecipe/
│   ├── updateRecipe/
│   ├── deleteRecipe/
│   ├── createRequest/
│   ├── updateRequestStatus/
│   └── toggleFavorite/
├── pages/
│   ├── home/
│   ├── recipe/
│   │   ├── detail/
│   │   └── edit/
│   ├── requests/
│   │   ├── list/
│   │   └── manage/
│   ├── user/
│   │   └── profile/
│   └── admin/
│       ├── admin/
│       ├── categories/
│       └── recipes/
├── components/
│   ├── recipe-card/
│   ├── recipe-form/
│   ├── request-card/
│   ├── empty-state/
│   ├── loading-state/
│   └── user-avatar/
├── services/
│   ├── cloud.js
│   ├── user-service.js
│   ├── recipe-service.js
│   ├── request-service.js
│   ├── favorite-service.js
│   └── upload-service.js
├── constants/
│   ├── collections.js
│   ├── roles.js
│   ├── request-status.js
│   └── recipe-status.js
├── utils/
│   ├── date.js
│   ├── format.js
│   ├── validator.js
│   └── error.js
├── images/
└── legacy/
    ├── server/
    └── old-pages/
```

说明：

1. `pages/` 只放当前小程序主线页面。
2. `components/` 放可复用 UI 单元，避免页面间复制。
3. `services/` 统一封装云数据库和云函数调用，页面不直接散写 `wx.cloud.callFunction`。
4. `constants/` 放集合名、角色、状态枚举，避免字符串到处写。
5. `legacy/` 临时保留旧实现，等迁移完成后再删除。
6. `server/` 不再作为第一版主链路，后续可移动到 `legacy/server/`。

## 4. 页面目录整理

### 4.1 首页

目标路径：

```text
pages/home/home.js
pages/home/home.wxml
pages/home/home.wxss
pages/home/home.json
```

职责：

1. 展示已发布菜谱列表。
2. 搜索菜谱。
3. 分类筛选。
4. 最新/热门排序。
5. 进入菜谱详情。

迁移来源：

```text
pages/menu/menu.*
pages/recipes/list/list.*
```

整理原则：

1. 不再叫 `menu`。
2. 不展示“月售”“好评率”“店铺”等商业字段。
3. 首页卡片统一使用 `recipe-card` 组件。

### 4.2 菜谱详情

目标路径：

```text
pages/recipe/detail/detail.js
pages/recipe/detail/detail.wxml
pages/recipe/detail/detail.wxss
pages/recipe/detail/detail.json
```

职责：

1. 展示菜谱主图、标题、作者、描述。
2. 展示食材和步骤。
3. 收藏菜谱。
4. 发起“想吃”请求。
5. 作者或管理员可以编辑菜谱。

迁移来源：

```text
pages/product/product.*
pages/recipes/detail/detail.*
```

整理原则：

1. 不再使用 `product`。
2. 不出现价格、规格、加入购物车。
3. 主按钮改成“想吃这个”。

### 4.3 发布和编辑菜谱

目标路径：

```text
pages/recipe/edit/edit.js
pages/recipe/edit/edit.wxml
pages/recipe/edit/edit.wxss
pages/recipe/edit/edit.json
```

职责：

1. 新建菜谱。
2. 编辑菜谱。
3. 上传主图。
4. 添加食材。
5. 添加步骤。
6. 保存草稿。
7. 发布菜谱。

迁移来源：

```text
pages/recipes/create/create.*
```

整理原则：

1. 新建和编辑共用同一页面。
2. 通过 `id` 参数判断编辑模式。
3. 表单组件抽到 `components/recipe-form/`。

### 4.4 我的请求

目标路径：

```text
pages/requests/list/list.js
pages/requests/list/list.wxml
pages/requests/list/list.wxss
pages/requests/list/list.json
```

职责：

1. 展示我发起的想吃请求。
2. 展示请求状态。
3. 取消待处理请求。
4. 跳回菜谱详情。

迁移来源：

```text
pages/orders/orders.*
pages/order-detail/order-detail.*
```

整理原则：

1. 不再叫 `orders`。
2. 请求状态使用 `request-status.js` 常量。
3. 列表卡片使用 `request-card` 组件。

### 4.5 收到的请求

目标路径：

```text
pages/requests/manage/manage.js
pages/requests/manage/manage.wxml
pages/requests/manage/manage.wxss
pages/requests/manage/manage.json
```

职责：

1. 作者查看自己收到的想吃请求。
2. 管理员查看全部请求。
3. 更新请求状态。

迁移来源：

```text
pages/admin/orders/orders.*
pages/order/order.*
```

整理原则：

1. 不再使用“订单管理”。
2. 状态流转只保留 `pending -> accepted -> preparing -> done` 和 `pending -> declined`。
3. 页面里不展示价格、总金额、取餐。

### 4.6 我的页面

目标路径：

```text
pages/user/profile/profile.js
pages/user/profile/profile.wxml
pages/user/profile/profile.wxss
pages/user/profile/profile.json
```

职责：

1. 展示用户资料。
2. 进入我发布的菜谱。
3. 进入我收藏的菜谱。
4. 进入我的请求。
5. 进入收到的请求。
6. 管理员显示管理入口。

迁移来源：

```text
pages/user/profile/profile.*
```

整理原则：

1. 保留当前页面，但去掉直接请求 Express 的写法。
2. 用户资料改为云开发用户模型。

### 4.7 管理页

目标路径：

```text
pages/admin/admin/admin.js
pages/admin/admin/admin.wxml
pages/admin/admin/admin.wxss
pages/admin/admin/admin.json
pages/admin/categories/categories.*
pages/admin/recipes/recipes.*
```

职责：

1. 管理分类。
2. 管理全部菜谱。
3. 查看全部请求入口。
4. 轻量统计。

迁移来源：

```text
pages/admin/admin.*
pages/admin/menu/menu.*
pages/admin/store-config/store-config.*
```

整理原则：

1. 后台只服务小范围管理，不做商业报表。
2. `store-config` 迁移为平台基础设置，第一版可以先不做。

## 5. 组件目录整理

建议新增：

```text
components/
├── recipe-card/
├── recipe-form/
├── request-card/
├── empty-state/
├── loading-state/
└── user-avatar/
```

### 5.1 recipe-card

使用页面：

```text
pages/home/home
pages/user/profile/profile
pages/admin/recipes/recipes
```

职责：

1. 展示封面。
2. 展示标题。
3. 展示作者。
4. 展示耗时、难度、想吃数。

### 5.2 recipe-form

使用页面：

```text
pages/recipe/edit/edit
```

职责：

1. 菜谱基础信息表单。
2. 食材编辑。
3. 步骤编辑。
4. 图片上传入口。

### 5.3 request-card

使用页面：

```text
pages/requests/list/list
pages/requests/manage/manage
```

职责：

1. 展示请求人。
2. 展示菜谱标题。
3. 展示原因。
4. 展示状态。
5. 展示可用操作。

### 5.4 empty-state

使用页面：

```text
所有列表页
```

职责：

1. 统一空状态。
2. 支持标题、说明、按钮。

### 5.5 loading-state

使用页面：

```text
所有异步页面
```

职责：

1. 统一加载状态。
2. 避免页面自己重复写 loading 逻辑。

### 5.6 user-avatar

使用页面：

```text
菜谱详情
评论区
我的页面
请求卡片
```

职责：

1. 展示头像。
2. 处理默认头像。
3. 展示昵称。

## 6. 服务层目录整理

建议新增：

```text
services/
├── cloud.js
├── user-service.js
├── recipe-service.js
├── request-service.js
├── favorite-service.js
└── upload-service.js
```

### 6.1 cloud.js

职责：

1. 初始化云开发。
2. 封装 `callFunction`。
3. 封装统一错误处理。
4. 导出数据库实例。

### 6.2 user-service.js

职责：

1. 登录。
2. 获取当前用户。
3. 更新用户资料。
4. 判断管理员权限。

### 6.3 recipe-service.js

职责：

1. 获取菜谱列表。
2. 获取菜谱详情。
3. 创建菜谱。
4. 更新菜谱。
5. 删除菜谱。
6. 增加浏览数。

### 6.4 request-service.js

职责：

1. 创建想吃请求。
2. 获取我发起的请求。
3. 获取我收到的请求。
4. 更新请求状态。
5. 取消请求。

### 6.5 favorite-service.js

职责：

1. 收藏菜谱。
2. 取消收藏。
3. 获取收藏列表。
4. 判断是否收藏。

### 6.6 upload-service.js

职责：

1. 上传菜谱图片。
2. 上传步骤图片。
3. 生成云存储路径。
4. 删除废弃图片。

## 7. 常量目录整理

建议新增：

```text
constants/
├── collections.js
├── roles.js
├── request-status.js
└── recipe-status.js
```

### 7.1 collections.js

示例：

```js
module.exports = {
  USERS: 'users',
  CATEGORIES: 'categories',
  RECIPES: 'recipes',
  REQUESTS: 'requests',
  FAVORITES: 'favorites',
  COMMENTS: 'comments'
}
```

### 7.2 roles.js

示例：

```js
module.exports = {
  USER: 'user',
  ADMIN: 'admin'
}
```

### 7.3 request-status.js

示例：

```js
module.exports = {
  PENDING: 'pending',
  ACCEPTED: 'accepted',
  PREPARING: 'preparing',
  DONE: 'done',
  DECLINED: 'declined',
  CANCELLED: 'cancelled'
}
```

### 7.4 recipe-status.js

示例：

```js
module.exports = {
  DRAFT: 'draft',
  PUBLISHED: 'published',
  HIDDEN: 'hidden',
  DELETED: 'deleted'
}
```

## 8. 云函数目录整理

建议新增：

```text
cloudfunctions/
├── login/
│   ├── index.js
│   └── package.json
├── createRecipe/
│   ├── index.js
│   └── package.json
├── updateRecipe/
│   ├── index.js
│   └── package.json
├── deleteRecipe/
│   ├── index.js
│   └── package.json
├── createRequest/
│   ├── index.js
│   └── package.json
├── updateRequestStatus/
│   ├── index.js
│   └── package.json
└── toggleFavorite/
    ├── index.js
    └── package.json
```

云函数命名原则：

1. 使用动词开头。
2. 一个云函数只负责一个清晰业务动作。
3. 权限校验放在云函数内。
4. 页面不直接写复杂权限判断。

## 9. 文档目录整理

建议新增：

```text
docs/
├── 菜谱小程序整体方案.md
├── 菜谱小程序文件架构整理.md
├── 云开发迁移步骤.md
├── 数据集合设计.md
└── 迁移记录.md
```

当前根目录旧文档：

```text
方案.md
新方案.md
升级改动方案.md
项目结构说明.md
VERDENT.md
```

处理建议：

1. `VERDENT.md` 保留在根目录，作为工具上下文。
2. `项目结构说明.md` 标记为旧版点单系统结构说明。
3. `方案.md`、`新方案.md`、`升级改动方案.md` 迁移到 `docs/legacy/`。
4. 新方案文档统一放入 `docs/`。

第一版可以先不移动旧文档，但新增文档应逐步收敛到 `docs/`。

## 10. 旧文件迁移映射

### 10.1 页面映射

```text
pages/menu/menu.*
  -> pages/home/home.*

pages/product/product.*
  -> pages/recipe/detail/detail.*

pages/recipes/detail/detail.*
  -> pages/recipe/detail/detail.* 的主要来源

pages/recipes/create/create.*
  -> pages/recipe/edit/edit.*

pages/orders/orders.*
  -> pages/requests/list/list.*

pages/order-detail/order-detail.*
  -> 合并进 pages/requests/list/list 或请求详情弹层

pages/order/order.*
  -> 合并进 pages/recipe/detail/detail 的“想吃请求”弹层

pages/cart/cart.*
  -> 第一版废弃；不再需要购物车/愿望清单页

pages/admin/orders/orders.*
  -> pages/requests/manage/manage.*

pages/admin/menu/menu.*
  -> pages/admin/recipes/recipes.* 和 pages/admin/categories/categories.*

pages/admin/store-config/store-config.*
  -> 第一版冻结
```

### 10.2 后端映射

```text
server/app.js
  -> legacy/server/app.js

server/routes/*
  -> legacy/server/routes/*

server/controllers/*
  -> legacy/server/controllers/*

server/models/*
  -> legacy/server/models/*

server/database/*
  -> legacy/server/database/*
```

后端功能迁移到：

```text
cloudfunctions/*
services/*
```

### 10.3 工具映射

```text
utils/util.js
  -> utils/date.js
  -> utils/format.js
  -> services/cloud.js
```

当前 `utils/util.js` 只适合保留日期格式化，不适合作为 API 请求中心。

## 11. app.json 页面注册目标

第一版目标页面顺序：

```json
{
  "pages": [
    "pages/home/home",
    "pages/recipe/detail/detail",
    "pages/recipe/edit/edit",
    "pages/requests/list/list",
    "pages/requests/manage/manage",
    "pages/user/profile/profile",
    "pages/admin/admin/admin",
    "pages/admin/categories/categories",
    "pages/admin/recipes/recipes"
  ],
  "tabBar": {
    "list": [
      {
        "pagePath": "pages/home/home",
        "text": "菜谱"
      },
      {
        "pagePath": "pages/requests/list/list",
        "text": "想吃"
      },
      {
        "pagePath": "pages/user/profile/profile",
        "text": "我的"
      }
    ]
  }
}
```

说明：

1. TabBar 保留三个入口即可。
2. 管理页不放 TabBar，由“我的”页面进入。
3. 发布菜谱可以放在首页右上角或我的页面入口。

## 12. 第一版需要冻结的目录

以下目录第一版先不继续开发：

```text
pages/blind-box/
pages/game/
pages/reminders/
server/
database/
```

冻结原因：

1. 它们不是第一版核心闭环。
2. 它们依赖旧后端或旧业务模型。
3. 继续维护会增加迁移成本。

冻结策略：

1. 不删文件。
2. 不在 `app.json` 新入口中注册。
3. 不继续修扩展功能里的细节 bug。
4. 核心闭环稳定后再判断是否恢复。

## 13. 第一版可以删除的业务概念

从新代码中删除或避免新增：

```text
product
cart
order
store
price
sales
stock
payment
delivery
pickup
checkout
```

统一替换为：

```text
recipe
request
favorite
category
author
profile
admin
```

## 14. 迁移步骤

### 第一步：建立新骨架

创建：

```text
cloudfunctions/
services/
constants/
components/
docs/
pages/home/
pages/recipe/
pages/requests/
```

不删除旧页面。

### 第二步：接入云开发

改造：

```text
app.js
services/cloud.js
cloudfunctions/login/
```

验收：

1. 小程序启动时能初始化云开发。
2. 能获取 openid。
3. 能创建或读取用户。

### 第三步：首页迁移

改造：

```text
pages/home/home.*
components/recipe-card/
services/recipe-service.js
constants/collections.js
```

验收：

1. 首页从云数据库读取菜谱。
2. 不再调用 Express API。
3. 不再显示点单系统字段。

### 第四步：菜谱详情和发布

改造：

```text
pages/recipe/detail/detail.*
pages/recipe/edit/edit.*
components/recipe-form/
services/upload-service.js
cloudfunctions/createRecipe/
cloudfunctions/updateRecipe/
```

验收：

1. 可以发布菜谱。
2. 可以查看详情。
3. 作者可以编辑。

### 第五步：想吃请求

改造：

```text
pages/requests/list/list.*
pages/requests/manage/manage.*
components/request-card/
services/request-service.js
cloudfunctions/createRequest/
cloudfunctions/updateRequestStatus/
constants/request-status.js
```

验收：

1. 用户可以点“想吃”。
2. 作者可以处理请求。
3. 请求状态能正确展示。

### 第六步：清理旧结构

当新主线稳定后：

```text
server/ -> legacy/server/
旧点单页面 -> legacy/old-pages/
旧文档 -> docs/legacy/
```

确认没有引用后再删除 legacy。

## 15. 文件移动规则

迁移时遵守：

1. 每次只迁移一条业务链路。
2. 迁移前先确认 `app.json` 页面注册。
3. 页面移动后同步更新所有 `wx.navigateTo`、`wx.switchTab` 路径。
4. 不混合迁移和视觉重构。
5. 不在同一次提交里同时移动大量文件和改业务逻辑。
6. 每次迁移后做一次微信开发者工具编译检查。

推荐提交粒度：

```text
1. 新增云开发基础结构
2. 迁移首页到云数据库
3. 迁移菜谱详情
4. 迁移发布菜谱
5. 迁移想吃请求
6. 冻结旧后端入口
```

## 16. 最终结构验收标准

当文件架构整理完成后，应满足：

1. `pages/` 目录只包含第一版正在使用的页面。
2. `app.json` 中没有旧点单页面。
3. 页面代码不再调用 `http://localhost:3000/api`。
4. 业务代码不再出现 `price`、`checkout`、`payment`、`sales`。
5. `server/` 不再是主链路依赖。
6. 云函数负责权限敏感写操作。
7. 页面通过 `services/` 访问数据。
8. 常量统一从 `constants/` 引入。
9. 旧文件要么在 `legacy/`，要么已删除。

## 17. 当前阶段建议

下一步不要直接全量移动文件。建议先做“新骨架 + 云开发登录”：

```text
新增 services/cloud.js
新增 constants/collections.js
新增 cloudfunctions/login/
改 app.js 接入 wx.cloud.init
```

然后再迁移首页。

这样能保证项目始终可运行，也避免旧点单系统和新菜谱系统在同一页面里继续纠缠。
