<view class="recipe-card {{compact ? 'compact' : ''}}" bindtap="handleTap">
  <image class="cover" src="{{recipe.coverImage || '/images/recipe.png'}}" mode="aspectFill"></image>
  <view class="body">
    <view class="title-row">
      <text class="title">{{recipe.title}}</text>
      <text wx:if="{{showStatus}}" class="status status-{{recipe.status}}">{{recipe.statusText || recipe.status}}</text>
    </view>
    <text class="desc">{{recipe.description || '朋友的私房菜谱'}}</text>
    <view class="meta">
      <text class="meta-text">{{recipe.cookingTime || 0}} 分钟</text>
      <text class="meta-text">{{recipe.difficultyText}}</text>
      <text class="meta-text">{{recipe.authorName}}</text>
    </view>
    <view class="stats">
      <text class="stat-text">{{recipe.wantCount || 0}} 想吃</text>
      <text class="stat-text">{{recipe.favoriteCount || 0}} 收藏</text>
      <text class="stat-text">{{recipe.viewCount || 0}} 浏览</text>
    </view>
  </view>
</view>
